# assets

Drop a background image here named `twice.jpg` (or `.png` / `.jpeg` / `.webp`)
and `app.py` will use it as the page background. If no image is present, the
app falls back to a pink gradient.
